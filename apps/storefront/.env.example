NEXT_PUBLIC_MEDUSA_BACKEND_URL=http://localhost:9000

# Set this to the token printed by `pnpm backend:seed`, or retrieve it in
# Medusa Admin at Settings > Publishable API keys after seeding.
NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=
